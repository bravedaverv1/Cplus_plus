// CONSTANTS
// where we dont want the values of a variable to change.

#include <iostream>

int main(){
  const double pi = 3.14;
  // pi = 0; // ! this causes an error if not commented out given the const assigned to pi var.
  // ? const , what is it?
  // const is a KEYWORD that triggers an error if its variable 
  // is attempted for change. in other words, const makes it so that 
  // you cannot change a variables value.
  return 0;
}