#include <iostream>
#include <vector>

  // * you can initizialize a vector with a value in 2 different ways.
  // example 1: // * std::vector<double> location = {42.651443, -73.749302};
  // Here, we are storing a latitude and a longitude.
  // example 2: // * std::vector<double> location(2);
  // the above is GREAT for when you know how many but not sure what you will store yet.

int main() {

  std::vector<double> subway_adult = {800, 1200, 1500};
  
  // Give subway_child some values:
  // ? PROMPT :
  // Initialize the subway_child vector with:
  // 400, 600, 750
  // std::vector<double> subway_child;
  std::vector<double> subway_child = {400, 600, 750};
  
}